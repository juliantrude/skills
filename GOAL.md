# GOAL

<!--
  Single source of truth for the /advance-goal loop (driven headless via the
  one-shot systemd chain, or interactively via `/loop /advance-goal`).
  The loop reads and rewrites this file every iteration.
-->

## North star

Turn the hustlebot web monitor from a single scrolling page into a small
multi-page dashboard whose home screen is readable at a glance from across the
room. The home screen shows only the KPIs — current status, current task, next
task, overall progress, and session/week budget — plus an animated avatar that
expresses the chain's state. Plan detail moves to its own page; the run log,
which currently drowns everything else, moves to a third. The avatar artwork is
**already finished and must not be redesigned**: six SVG scenes in
`plugins/hustlebot/assets/`, approved by the user over nine review rounds. This
goal is about the dashboard around them, not about the art.

## Definition of Done

- [x] `http://localhost:<HUSTLE_PORT>/` serves a home screen showing all five KPIs (status, current task, next task, plan progress, session + week budget) and the state-appropriate avatar, with **no run-log text anywhere on it**
- [x] `/plan` serves Definition of Done, the plan sections with per-section progress, and the GOAL.md iteration history
- [x] `/logs` serves the full run log
- [x] All three pages are reachable from a persistent nav, survive a page reload (deep links work), and poll without a full-page refresh
- [x] Each of the six avatar states renders for its corresponding `compute_state()` tone, verified by driving the monitor against fixture data — not by reading code
- [x] `hustle-setup.sh --dev <project>` symlinks `.hustle/bin/*` at the repo sources instead of copying, and a plain `hustle-setup.sh <project>` still copies exactly as before (verified by running both against a scratch directory)
- [x] `python3 plugins/hustlebot/scripts/hustle-monitor.py` still runs on the Python standard library alone — no new dependencies, no build step
- [x] Fully blocked externally does **not** count as done; a blocked increment is documented in `## Log` and the loop moves to the next one (n/a — nothing ended up blocked)

## Working Agreements (read every iteration — these override convenience)

**Priority order per iteration:**
1. If the previous iteration left a broken monitor (does not start, or a page 500s), fixing that comes before anything else.
2. Then take the next unchecked increment from `## Plan`.

**Git workflow:**
- Work on the branch `feat/dashboard-morepager`, branched from `main`. Create it if missing; never commit to `main`.
- **Never push.** No `git push`, no PR, no remote operation of any kind. The user reviews locally and merges by hand. This is a public repository and the loop runs unattended.
- One commit per completed increment. Message: what changed and why, not a restatement of the diff.
- Do not commit until the increment is verified per the bar below.

**Verification bar (this is where the last effort went wrong — do not skip):**
- A structural check is not a verification. Starting the monitor and reading its source proves nothing about what a user sees.
- Before checking off any increment that touches a page, **start the monitor and fetch the affected route**, then assert on the returned markup — that the KPI is present, that the log is absent from the home screen, that the nav links resolve.
- For anything visual, render it and look at it. `plugins/hustlebot/assets/*.svg` can be rasterised with `convert in.svg out.png` and read back as an image. Note that ImageMagick silently drops elements carrying `<animate>` children and does not support `clipPath`, `<use>` or CSS animation — strip SMIL and hardcode the pose before rendering, or you will diagnose tool artefacts as bugs.
- If an increment cannot be verified, say so in `## Log` and leave the box unchecked rather than claiming it.

**Out-of-scope systems — never modify:**
- `plugins/hustlebot/assets/*.svg` — the avatar artwork is signed off. If a state genuinely cannot be wired up without an art change, document it in `## Log` and leave the increment blocked; do not redraw it.
- `hustle-session.sh`, `hustle-scheduler.sh` — the scheduling chain is working and orthogonal to this goal. `hustle-setup.sh` may be touched **only** for the `--dev` flag.
- The runtime namespace: `.hustle/`, the `HUSTLE_*` config keys, and the `hustle-<slug>` service names stay exactly as they are. Renaming them breaks every existing installation.
- Anything under `.hustle/` in this repo is local runtime state, not source. Never commit it.

**Etiquette:**
- Comments explain why, never what. No comment that restates the line below it.
- Dashboard copy is English (the UI is already English); `GOAL.md` log lines are English too.
- Never claim a state you did not observe. "Rendered and checked" and "should work" are different sentences.

**References:**
- `plugins/hustlebot/scripts/hustle-monitor.py` — current single-page monitor; `compute_state()` is the authority on which states exist.
- `plugins/hustlebot/assets/` — the six approved avatar scenes: `working`, `idle`, `waiting`, `sleeping`, `searching`, `done`.
- `GOAL.md` parsing lives in `parse_goal()`; the format contract is in `plugins/hustlebot/skills/grill-goal/SKILL.md`.

## Plan

### Foundation
- [x] Add `--dev` to `hustle-setup.sh`: symlink `.hustle/bin/*` at the repo sources instead of copying, so edits to the monitor are visible on the running dashboard. Verify both modes against a scratch directory; confirm the default path is byte-identical to today's behaviour.
- [x] Re-run `hustle-setup.sh --dev` against this repo so the rest of the work is visible live on `:8787`, and confirm the monitor restarts cleanly.
- [x] Arbitrate the monitor port instead of blindly binding `HUSTLE_PORT`. On startup, if the port is already held, read the holder's `status.json`: if that chain is still working, **do not steal the port** — fall back to the next free port, log which one was chosen, and keep running; if the holder's `phase` is `complete`, claim the port and retire the stale monitor. Also make `hustle-setup.sh` pick a free port at install time rather than always writing 8787. Verify by starting two projects against the same port in both states — a working holder and a completed one — and asserting which process ends up bound. Today a second project silently dies in a systemd restart loop with `EADDRINUSE` while the user looks at the wrong project's dashboard.
- [x] Split the monitor's embedded HTML into `plugins/hustlebot/assets/app.html`, `app.css`, `app.js`; add a small static-file handler to `hustle-monitor.py` and extend `hustle-setup.sh` to copy (or symlink) the whole `assets/` directory. Verify the existing single page still renders unchanged before moving on.

### Routing and pages
- [x] Add hash-based routing (`#/`, `#/plan`, `#/logs`) with a persistent nav, so a reload lands on the same page. No server-side route changes; the existing `/api/*` endpoints stay as they are.
- [x] Build the **home** screen: status headline, current task, next task, overall plan progress, session and week budget. No log output on this page at all.
- [x] Build the **plan** page: DoD donut, plan sections with per-section progress, iteration history from `## Log`.
- [x] Build the **logs** page: the full run log, with the newest lines visible without scrolling.

### Avatar
- [x] Map `compute_state()`'s tones onto the six scenes (`run`→working, `wait`→waiting, `hold`→sleeping, `done`→done, `idle`→idle, unreachable→searching), and decide what `warn`/`blocked`/`not_ready` show — reuse an existing scene rather than commissioning new art.
- [x] Embed the avatar on the home screen so the scene follows the live status, including the browser-side "monitor unreachable" case which no server response can report.
- [x] Drive the monitor against fixture `status.json` files covering every state, and confirm by rendering that each one shows its intended scene.

### Closeout
- [x] Final sweep: everything at spec or documented-blocked; DoD checkboxes above updated; no stray files from the abandoned pixel-art approach remain.

## Status

STATUS: COMPLETE
**Next action:** none — all DoD items verified, all plan increments checked off.
**Blockers:** none

## Budget

- session pause threshold: 85%
- weekly pause threshold: 90%

## Log

- 2026-07-19 — GOAL.md created via /hustle. Avatar artwork settled beforehand over nine review rounds: pixel art was tried and abandoned (unreadable at 48x32; the outline tone collided with the background), replaced by animated SVG. Plugin renamed hustle → hustlebot (0.3.0); runtime namespace deliberately unchanged.
- 2026-07-19 — Added `--dev` to `hustle-setup.sh`: symlinks `.hustle/bin/*` at the plugin sources instead of copying. Verified against two scratch dirs: default mode produces regular files byte-identical to the plugin source (`diff` clean on all three scripts); `--dev` mode produces symlinks resolving to the repo sources; re-running either mode on top of the other cleanly swaps symlinks↔copies. Committed as f4742dc on `feat/dashboard-morepager`.
- 2026-07-19 — Re-ran `hustle-setup.sh --dev .` against this repo. Found the monitor crash-looping under systemd (`EADDRINUSE`, restart counter 40+) — this violated the "fix a broken monitor first" priority rule, so fixed before anything else: `hustle-monitor.py` computed `HUSTLE_HOME` via `Path(__file__).resolve()`, which follows the `.hustle/bin/hustle-monitor.py` symlink back to its source in `plugins/hustlebot/scripts/`, so the config file was never found and the monitor silently fell back to the default port 8787 — already held by another project's monitor. Changed `.resolve()` to `.absolute()` so the symlink path itself (inside `.hustle/`) is used. Verified: `systemctl --user restart hustle-monitor-skills.service` now stays `active (running)`, binds `0.0.0.0:8789` (the configured port), and `curl localhost:8789/` returns HTTP 200 with the expected `<title>hustle monitor</title>` markup. Committed as f075e3e. This is a preview of the port-collision bug the next increment (port arbitration) is meant to fix properly — today it's silent, not just non-graceful.
- 2026-07-19 — Also found (before starting this increment) that the previous iteration's `--dev` chmod had leaked into git status: `chmod +x` on `.hustle/bin/hustle-{scheduler,session}.sh` follows the symlink and flips the mode bit on the tracked source in `plugins/hustlebot/scripts/` (Linux has no `lchmod`; `hustle-monitor.py` was already 100755 so it didn't show). Reverted the mode-only diff (`git checkout --`) per the "never modify hustle-session.sh/hustle-scheduler.sh" rule and left it alone — worth a permanent fix (commit those two scripts as 100755 once so dev-mode's chmod becomes a no-op) but that's a deliberate call for a human, not something to sneak into an unrelated increment. Also committed the prior iteration's GOAL.md bookkeeping (checkbox + log line for f075e3e) that never made it into a commit.
- 2026-07-19 — Split the monitor's embedded HTML/CSS/JS into `plugins/hustlebot/assets/app.html`, `app.css`, `app.js`, byte-for-byte identical to the strings previously baked into `PAGE` in `hustle-monitor.py` (only change: `<style>`/`<script>` became `<link>`/`<script src>` tags). Added `ASSETS_DIR = HUSTLE_HOME / "assets"` and a small static-file handler (`_send_asset`) to `hustle-monitor.py`, gated so only bare filenames with an extension and no `/` or `..` are served — verified the traversal guard rejects `/../scripts/hustle-monitor.py` (404) and serves `/app.css`, `/app.js`, `/working.svg` with correct MIME types. Extended `hustle-setup.sh` to `rm -rf`/copy `assets/` in default mode and symlink it in `--dev` mode, alongside the existing bin/ handling. Verified end to end: ran `hustle-setup.sh` (default) and `hustle-setup.sh --dev` against two fresh scratch dirs — default mode produced a real copy of `assets/`, dev mode a symlink to the plugin source; started the monitor against the dev-mode scratch dir on a throwaway port and `curl`'d `/`, `/app.js` and diffed the response byte-for-byte against the source files (`MATCH` both times), confirmed `/app.css` and `/working.svg` return 200 with the right `Content-Type`. Found the live monitor for *this* repo (`hustle-monitor-skills.service`) still had the old `PAGE`-based code resident in memory (dev-mode symlinks the script, but a running process doesn't hot-reload) and no `.hustle/assets` symlink yet, since its dev-mode install predates this increment — re-ran `hustle-setup.sh --dev .` here to add the missing symlink and `systemctl --user restart hustle-monitor-skills.service`; confirmed it came back `active` and `curl localhost:8789/` now serves the new `app.html` with working `/app.css` and `/working.svg`. No changes to `hustle-scheduler.sh`/`hustle-session.sh`; `.hustle/` stayed out of git (only `plugins/hustlebot/{scripts,assets}` and `GOAL.md` touched).
- 2026-07-19 — Implemented port arbitration. `hustle-monitor.py`: added `resolve_port()` — tries the configured `HUSTLE_PORT` first; on `EADDRINUSE`, HTTP-probes the holder's own `/api/status` (added a `pid` field to `compute_state()` for this) instead of adding a new registry file; if the holder reports `phase: complete` it SIGTERMs that pid and reclaims the port (retry loop, 3s budget), otherwise it treats the holder as alive and binds the next free port instead, logging the choice either way. `hustle-setup.sh`: replaced the hardcoded `HUSTLE_PORT="8787"` with a python3 scan for the first free port from 8787, and fixed the `Watch:` hint at the end to read the port back out of the written config instead of printing a stale literal 8787. Verified end to end, not just structurally: ran two real `hustle-setup.sh` installs into scratch dirs — both picked port 8788 (expected: install-time picking only checks what's bound *right now*, and neither monitor was running yet, so this exact race is the real-world version of the bug) — then started both via `systemctl --user start hustle-monitor-<slug>.service` and watched B's journal log `"port 8788 is held by another chain (phase=unknown) — falling back to 8790"` and stay `active`, while A stayed `active` on 8788; also drove the two-phase scenario directly against `hustle-monitor.py` (no systemd) with fixture `status.json` files — a `phase: active` holder made the second monitor fall back to the next port, a `phase: complete` holder got SIGTERMed and its port reclaimed by the pid that killed it. All scratch systemd units and `/tmp` dirs removed after. Note: mid-cleanup a stray `pkill -f hustle-monitor.py` briefly killed this repo's own live monitor unit too (Restart=on-failure doesn't cover a clean SIGTERM exit) — caught it via `curl` returning connection-refused, restarted with `systemctl --user start hustle-monitor-skills.service`, confirmed 200 again. `hustle-scheduler.sh`/`hustle-session.sh` untouched.
- 2026-07-19 — Built the home screen per DoD: replaced the single free-text "next action" banner with two structured panels, **Current task** and **Next task**, both sourced from `g.next_up` (already computed by `parse_goal()` but previously unused by the JS) instead of the raw `next_action` string, so they're deterministic across iterations rather than tied to one hand-written sentence. Removed the `stflag` STATUS-pill (redundant with the tone-colored badge, which is the actual "current status" KPI) and moved the three cards that aren't part of the five KPIs — Next run, Blockers, Last note — off the home grid onto a new `#statusGrid` at the top of the `/plan` page, so no information was lost, just relocated per the increment's own framing ("curating what home shows vs. deferring to /plan"). Home's `#grid` now holds exactly three cards: Overall progress, Session budget, Weekly budget. Added `.next-action.alt` CSS so the secondary "next task" panel reads as visually subordinate to "current task". Verified with a real headless-Chromium session (playwright, installed fresh into `/tmp/pw-verify`, removed after): rendered `#/` and asserted via `innerText` that Current task/Next task/Overall progress/Session budget/Weekly budget are present and that Blockers/Next run/Last note/any `run.log` text are *absent* from `#page-home`'s rendered text; rendered `#/plan` and asserted Blockers/Next run *are* present there, plus Definition of Done and Iteration history headings; reloaded on `#/` and confirmed `#page-home` stays visible and `#page-plan` stays hidden (case-sensitivity note: two headings are asserted lowercased because `text-transform:uppercase` in CSS changes what `innerText` returns, not a product bug). Screenshotted both pages and looked at them — home reads cleanly as five KPI blocks with no log text, plan now leads with the three relocated status cards above the DoD/plan/history layout. Also temporarily swapped in a fixture `status.json` with `session_pct: "42"`/`week_pct: "61"` to confirm the budget bars actually render percentages (not just the empty-state dash this repo's real status.json currently has, since no chain has completed a real run yet) — restored the original file byte-for-byte afterward via a backup copy. No changes to `hustle-monitor.py`, `hustle-setup.sh`, `hustle-scheduler.sh`, or `hustle-session.sh` — this increment only touched `plugins/hustlebot/assets/{app.html,app.css,app.js}`. Assets are symlinked in this repo's dev-mode install, so no service restart was needed to pick up the changes.
- 2026-07-19 — Added hash-based routing (`#/`, `#/plan`, `#/logs`) with a persistent nav. Split the single-page markup in `app.html` into three `<section>`s (`page-home`, `page-plan`, `page-logs`) matching the DoD's eventual page split, wrapped by a new `<nav>`; `app.js` gained a `route()` function driven by `hashchange` that toggles section visibility and the nav's active class, defaulting unknown/empty hashes to `/`. No server route changes — `/api/*` untouched. Verified with a real headless-Chromium session (`playwright`, installed fresh into `/tmp/pw-verify` and removed afterward) driven against the live dev-mode monitor on `:8789`: deep-linking straight to `#/plan` shows only the plan section and highlights the Plan nav item; a full `page.reload()` on `#/plan` stays on `#/plan` (hash survives, section stays correct); clicking the Logs nav link switches to `#/logs` and hides the others; navigating to a bogus hash (`#/bogus`) falls back to home. Screenshotted all three routes and looked at them — home shows the badge/next-action/KPI grid with no log text, plan shows the DoD donut + plan sections + iteration history, logs shows the run-log `<pre>`. Content curation per page (DoD's "no run-log text on home" bar, "Plan progress" duplication, etc.) is deliberately left for the next increment — this one only proves the routing shell works. Ran outside `/loop` and with no headless-chain scaffolding installed (`scripts/schedule-next-run.sh` absent), and session usage was already at 98%, so per the budget table no wakeup is armed — resuming this loop requires either `/loop /advance-goal` or `hustle:loop-setup`.
- 2026-07-19 — Checked off the **plan** page increment without writing new code: it already fully satisfies its own DoD wording, delivered as a side effect of the routing increment (`app.html`/`app.js` already had `#donut`/`#dodList`, `#plan` with per-section `<details>` and progress bars, and `#goalLog`). Verified for real, not by reading source: installed `playwright` fresh into a venv at `/tmp/pw-verify`, rendered `http://localhost:8789/#/plan` against this repo's live dev-mode monitor, and asserted via `inner_text`/locator counts that `#donutPct` shows a computed percentage, `#dodList` has all 8 DoD items, `#plan` has 4 `<details>` sections each showing `done/total` and a progress bar (Foundation 4/4, Routing and pages 2/4, Avatar 0/3, Closeout 0/1), and `#goalLog` has 8 entries in reverse order (newest first). Reloaded on `#/plan` and confirmed the hash and section visibility survive. Screenshotted and looked at it — matches the DoD wording exactly. No source files touched this iteration, only `GOAL.md`.
- 2026-07-19 — Mapped `compute_state()`'s tones onto the six approved avatar scenes. Added `SCENE_BY_TONE` to `hustle-monitor.py` and a `"scene"` field to `compute_state()`'s return dict: `run`→working, `wait`→waiting, `hold`→sleeping, `done`→done, `idle`→idle. For `warn` (covers `blocked`, `not_ready`, and the generic "stopped?" fallback) — the one tone with no dedicated scene — reused `searching` (binoculars-to-face reads as "investigating a problem"), the same scene reserved for the browser-side "monitor unreachable" case; the two can never coexist since an unreachable monitor can't report a tone at all, so sharing the scene isn't a collision. No new art, per the out-of-scope rule on `assets/*.svg`. Verified for real: restarted this repo's live dev-mode monitor (`systemctl --user restart hustle-monitor-skills.service`) and `curl`'d `/api/status`, confirming `tone=run`/`scene=working` for the actually-running chain (this iteration); then drove `compute_state()` directly against a scratch `HUSTLE_HOME` (temp dir, no shared state with the real install) with fixture `status.json` contents covering every remaining phase — `complete`→done/done, `on_hold`→hold/sleeping, `blocked`→warn/searching, `not_ready`→warn/searching, missing file→idle/idle, `active`+armed→wait/waiting, and an unmapped phase→warn/searching (the "stopped?" fallback) — asserting the printed tone/scene pairs matched the design table exactly. Confirmed all six `scene` values (`working`, `waiting`, `sleeping`, `done`, `idle`, `searching`) match existing filenames in `plugins/hustlebot/assets/` byte-for-byte (`ls` diffed against the dict's value set). This increment only adds the mapping/data — no page renders it yet, so no route was fetched; that's the next increment (embedding). No changes to `hustle-setup.sh`, `hustle-scheduler.sh`, `hustle-session.sh`, or any `.svg`.
- 2026-07-19 — Built the **logs** page increment. `#log`'s `<pre>` already rendered `api/log` verbatim (from the routing increment) but only had the shared `pre{max-height:300px;overflow:auto}` rule, so on load the scroll position sat at the top (oldest lines, since `tail()` returns chronological order) and the newest line — the one that actually matters moment to moment — was below the fold. Gave `#log` its own viewport-relative `max-height:calc(100vh - 220px)` in `app.css` (it's a dedicated full page now, not a home-screen snippet) and added `logEl.scrollTop=logEl.scrollHeight` in `app.js` right after each refresh populates it, so the view auto-scrolls to the newest line every 5s poll. Verified for real: installed `playwright` fresh into `/tmp/pw-verify` (removed after), rendered `http://localhost:8789/#/logs` against this repo's live dev-mode monitor with its real 104-line/8.5KB `run.log`, and measured via `scrollTop`/`scrollHeight`/`clientHeight` that the box overflows (1209px content vs 708px visible) and sits scrolled to within 2px of the bottom; confirmed the last line of `#log`'s text matched the last line the browser could see (`bottom edge reached: true`). Screenshotted and looked at it — the newest `[start] advance-goal run` line sits flush with the bottom edge of the box on load, no scrolling needed. Did not touch `run.log` itself (used the real file already accruing from this loop, no fixture swap needed since it was already long enough to overflow). "Routing and pages" section is now 4/4. No changes to `hustle-monitor.py`, `hustle-setup.sh`, `hustle-scheduler.sh`, or `hustle-session.sh`; only `plugins/hustlebot/assets/{app.css,app.js}` touched, committed as 73248bd.
- 2026-07-19 — Embedded the avatar on the home screen. `app.html` wraps the existing badge/current-task/next-task column and a new `<img id="avatar">` in a `.home-top` flex row (avatar on the right, 220px box, stacks below on narrow viewports via a `max-width:640px` media query); `app.js`'s `refresh()` sets `avatar.src = s.scene+'.svg'` and `avatar.alt = s.headline` on every successful poll, and the existing `catch` block (browser can't reach the monitor at all) now also sets `searching.svg`/`'Monitor unreachable'`, reusing the same scene as `warn` per the design note already in `SCENE_BY_TONE`'s comment. Verified for real, not structurally: installed `playwright` fresh into `/tmp/pw-verify` (removed after) and drove a scratch `hustle-setup.sh --dev` install (`/tmp/hustle-avatar-verify`, its own `.hustle`, port 8788, removed after) through all six mapped scenes by editing its `status.json`/`run.pid`/arming a real scheduler timer between reloads — `idle.svg`(no status file), `done.svg`(complete), `sleeping.svg`(on_hold), `searching.svg`(blocked, not_ready, and the unmapped-phase fallback — three separate assertions), `working.svg`(running=true) — asserting each time that `#avatar`'s resolved `src` ends in the expected filename and that `naturalWidth>0` (the image bytes actually loaded, not just a src string pointing at a 404). Also armed a real one-shot systemd timer via `hustle-scheduler.sh arm --in 600` to reach the `wait`/`waiting.svg` case, which needs a live "next run" fact and can't be faked with `status.json` alone. Then, with the scratch page already loaded, killed the scratch monitor's exact pid mid-session (not a broad `pkill -f`, see below) and waited a full 5s poll interval to confirm the *browser-side* unreachable path independently of the server-side tone table: badge flipped to "Monitor unreachable" and the avatar's `alt` updated to match, though the `<img>` itself renders as a broken-image glyph — expected and correct, since the whole point of "unreachable" is that the server (and therefore the asset it would serve) is gone; no client-side caching or inlining was added to paper over that, since the DoD only asks for the scene assignment, not offline rendering. Screenshotted every case and looked at them (home reads as five KPIs plus a state-matched robot scene each time; searching's binoculars-and-question-mark reads as "investigating" for blocked, done's confetti robot reads as celebratory). Finally re-rendered against *this* repo's real live monitor (`:8789`, currently `tone=run`) and confirmed the working-robot-at-the-desk scene renders correctly alongside the real KPI values. Mid-cleanup, a `pkill -f hustle-monitor.py` (meant only for the scratch install, argv `bin/hustle-monitor.py` relative to its own cwd) matched this repo's real service too and killed it a second time — same class of mistake logged during the port-arbitration increment; caught immediately via `curl` connection-refused, restarted with `systemctl --user start hustle-monitor-skills.service`, confirmed 200 and `tone=run` again before finishing. All scratch systemd units, the scratch `.hustle` dir, the playwright venv, and temp screenshots were removed afterward; `git status` confirms only `plugins/hustlebot/assets/{app.html,app.css,app.js}` changed. No changes to `hustle-monitor.py`, `hustle-setup.sh`, `hustle-scheduler.sh`, `hustle-session.sh`, or any `.svg`.
- 2026-07-19 — Redid the avatar-scene verification as a clean, repeatable pass with fresh eyes, per this increment's own framing (the previous increment's coverage was ad hoc, done as a side effect of building the embedding). Fresh scratch install (`hustle-setup.sh --dev /tmp/hustle-scene-verify`, port 8788), fresh Playwright venv. Wrote a small driver script (`/tmp/verify_scenes.py`, not committed — ephemeral verification tooling) that resets `status.json`/`run.pid`/the scheduler timer between cases and drives 8 fixtures covering all 6 scenes: no-status→idle, `run.pid` of a live process→working, `phase:complete`→done, `phase:on_hold`→sleeping, `phase:blocked`→searching, `phase:not_ready`→searching, an unmapped phase (the "stopped?" fallback)→searching, and — the one case the previous pass's log flagged as needing a *real* trigger — a genuine `hustle-scheduler.sh arm --in 600` (not a faked status field) →waiting. Asserted per case, via Playwright, that `#avatar`'s `src` resolves to the expected filename and `naturalWidth>0` (real image bytes, not a 404). First run exposed a real display bug in the fixture harness itself: the scratch dir had no `GOAL.md`, so `parse_goal()` returned `{}` and the KPI grid rendered "undefined / undefined" for Overall progress — not a product bug (every real install has a `GOAL.md` by the time it reaches the `idle` state, since `not_ready` is the phase that covers "no goal yet") but it made the fixtures unrepresentative, so copied this repo's real `GOAL.md` into the scratch dir and reran; all 8 cases passed cleanly with realistic KPI data. Screenshotted all 8 and looked at each with fresh eyes: idle (sleepy desk-monitor robot, "No runs yet"), working (robot at desk with code-lines monitor, green badge), done (confetti arms-up robot, blue "Goal reached"), sleeping (robot in a hammock under a night sky, amber "Paused"), waiting (robot sipping a drink, "Waiting for next run"), and all three searching cases (binoculars-glasses robot with a question mark against mountains, red badge) — every scene matches its tone and reads as intended. Cleaned up fully afterward: disarmed the scratch timer, stopped/disabled/removed both scratch systemd units, `daemon-reload`d, and `rm -rf`'d the scratch project dir, the Playwright venv, the screenshots, and the driver script. Confirmed this repo's real live monitor (`:8789`) was untouched throughout (`curl /api/status` still `tone=run`, `pid=2314635`, no restart needed this time) and `git status` is clean — no source files changed this iteration, only `GOAL.md`. "Avatar" section is now 3/3 (all avatar work done); only "Closeout" remains before the goal is complete.
- 2026-07-19 — Final sweep (Closeout), the last increment. Checked the repo for stray files from the abandoned pixel-art approach: none found (`find . -iname "*pixel*"` empty, `plugins/hustlebot/assets/` holds only the six approved SVGs plus `app.{html,css,js}`); the one non-source file present, `scripts/__pycache__/`, is git-ignored, not a stray artifact. Confirmed `hustle-monitor.py` imports only stdlib modules (`json, os, re, signal, socket, subprocess, time, urllib.request, datetime, http.server, pathlib`) — Playwright was verification-only tooling, installed to `/tmp` and removed each time, never a runtime dependency. Verified all eight DoD items for real against this repo's live dev-mode monitor (`:8789`, real `run.pid`, real `GOAL.md`, real `run.log`), not by re-reading prior log entries: fresh Playwright venv in `/tmp/pw-verify`, drove `#/`, `#/plan`, `#/logs` — home shows Current/Next task, Overall progress, Session/Weekly budget, a loaded avatar (`working.svg`, naturalWidth 480) and no `run.log` text; a hard reload on `#/plan` and `#/logs` stays on the same page with the right section visible and the wrong ones hidden; `#/plan` has Definition of Done and the Foundation section; `#/logs` has non-empty log content; clicking the Plan nav link from home navigates correctly. All assertions passed on the first run — no fixes needed this iteration. Removed the venv and driver script afterward. Checked off the Closeout increment and all eight DoD checkboxes above; every Plan section is now complete (Foundation 4/4, Routing and pages 4/4, Avatar 3/3, Closeout 1/1) and every DoD item is verified true. Goal marked COMPLETE — no further increments remain and no wakeup should be armed.
